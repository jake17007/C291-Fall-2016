#include <stdio.h>

int main(int theNumber) {
  
  // i is the potential prime number
  int i;
  for (i = 0; i < 100; i++) {
    int j = 0;
    int prime = 1;
    while (prime != 0) {
      if (i % j == 0)
	prime = 0;
      j++;
    }
    */
    if (prime == 1)
      printf("%d\n",i);
  }
}
